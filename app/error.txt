<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Something went wrong</title>
  <style>
    :root {
      color-scheme: light dark;
      --bg: #0b1020;
      --card: rgba(255, 255, 255, 0.08);
      --text: #e8ecff;
      --muted: #b6c0e0;
      --accent: #7c9cff;
      --danger: #ff5d7a;
      --border: rgba(255, 255, 255, 0.18);
    }

    * {
      box-sizing: border-box;
    }

    body {
      margin: 0;
      min-height: 100vh;
      display: grid;
      place-items: center;
      font-family: Inter, "Segoe UI", Roboto, Arial, sans-serif;
      background:
        radial-gradient(circle at 20% 20%, #1b2660 0%, transparent 40%),
        radial-gradient(circle at 80% 80%, #2f0f4e 0%, transparent 40%),
        var(--bg);
      color: var(--text);
      padding: 24px;
    }

    .error-card {
      width: min(680px, 100%);
      background: var(--card);
      border: 1px solid var(--border);
      border-radius: 20px;
      padding: 32px;
      backdrop-filter: blur(12px);
      box-shadow: 0 20px 50px rgba(0, 0, 0, 0.4);
    }

    .badge {
      display: inline-flex;
      align-items: center;
      gap: 8px;
      background: rgba(255, 93, 122, 0.14);
      color: #ffd5dd;
      border: 1px solid rgba(255, 93, 122, 0.4);
      border-radius: 999px;
      padding: 6px 12px;
      font-size: 12px;
      letter-spacing: 0.08em;
      text-transform: uppercase;
      font-weight: 600;
    }

    h1 {
      margin: 16px 0 8px;
      font-size: clamp(26px, 5vw, 40px);
      line-height: 1.1;
    }

    p {
      margin: 0;
      color: var(--muted);
      font-size: 16px;
      line-height: 1.6;
    }

    .details {
      margin-top: 18px;
      padding: 14px 16px;
      border-radius: 12px;
      border: 1px dashed rgba(255, 255, 255, 0.28);
      background: rgba(255, 255, 255, 0.04);
      font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, monospace;
      font-size: 14px;
      color: #d8deff;
    }

    .actions {
      margin-top: 24px;
      display: flex;
      flex-wrap: wrap;
      gap: 12px;
    }

    .btn {
      border: 0;
      border-radius: 12px;
      padding: 10px 16px;
      font-size: 14px;
      font-weight: 600;
      cursor: pointer;
      transition: transform 0.2s ease, opacity 0.2s ease;
    }

    .btn:hover {
      transform: translateY(-1px);
    }

    .btn-primary {
      background: linear-gradient(135deg, var(--accent), #5a7fff);
      color: white;
    }

    .btn-secondary {
      background: rgba(255, 255, 255, 0.1);
      color: var(--text);
      border: 1px solid var(--border);
    }
  </style>
</head>
<body>
  <main class="error-card">
    <span class="badge">Error 500</span>
    <h1>Something went wrong</h1>
    <p>
      We could not complete your request right now. Please try again in a moment,
      or go back to the homepage.
    </p>

    <div class="details">
      request_id: ERR-2026-0422-X1<br />
      message: Internal server error
    </div>

    <div class="actions">
      <button class="btn btn-primary" onclick="location.reload()">Try again</button>
      <button class="btn btn-secondary" onclick="window.location.href='/'">Go home</button>
    </div>
  </main>
</body>
</html>