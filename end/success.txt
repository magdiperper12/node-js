<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Success</title>
  <style>
    :root {
      --bg: #0a1226;
      --card: rgba(255, 255, 255, 0.08);
      --border: rgba(255, 255, 255, 0.2);
      --text: #e9eeff;
      --muted: #b6c3e6;
      --ok: #3ddc97;
      --ok-dark: #16a66d;
      --glow: rgba(61, 220, 151, 0.45);
    }

    * {
      box-sizing: border-box;
    }

    body {
      margin: 0;
      min-height: 100vh;
      display: grid;
      place-items: center;
      font-family: Inter, "Segoe UI", Roboto, Arial, sans-serif;
      color: var(--text);
      padding: 24px;
      background:
        radial-gradient(circle at 15% 15%, #1c2f75 0%, transparent 42%),
        radial-gradient(circle at 85% 85%, #1e5a4a 0%, transparent 40%),
        var(--bg);
    }

    .success-card {
      width: min(720px, 100%);
      padding: 36px;
      border-radius: 22px;
      border: 1px solid var(--border);
      background: var(--card);
      backdrop-filter: blur(12px);
      box-shadow: 0 24px 70px rgba(0, 0, 0, 0.35);
    }

    .icon {
      width: 60px;
      height: 60px;
      border-radius: 50%;
      display: grid;
      place-items: center;
      margin-bottom: 18px;
      background: linear-gradient(135deg, var(--ok), var(--ok-dark));
      box-shadow: 0 0 0 8px var(--glow);
      color: #06110d;
      font-size: 28px;
      font-weight: 800;
    }

    .badge {
      display: inline-flex;
      align-items: center;
      gap: 8px;
      padding: 6px 12px;
      border-radius: 999px;
      font-size: 12px;
      letter-spacing: 0.08em;
      text-transform: uppercase;
      font-weight: 700;
      color: #cbffe8;
      background: rgba(61, 220, 151, 0.17);
      border: 1px solid rgba(61, 220, 151, 0.45);
    }

    h1 {
      margin: 16px 0 10px;
      font-size: clamp(26px, 4.6vw, 42px);
      line-height: 1.1;
    }

    p {
      margin: 0;
      color: var(--muted);
      line-height: 1.6;
      font-size: 16px;
    }

    .actions {
      margin-top: 26px;
      display: flex;
      gap: 12px;
      flex-wrap: wrap;
    }

    .btn {
      border: 0;
      border-radius: 12px;
      padding: 10px 18px;
      font-size: 14px;
      font-weight: 700;
      cursor: pointer;
      transition: transform 0.2s ease, opacity 0.2s ease;
    }

    .btn:hover {
      transform: translateY(-1px);
      opacity: 0.95;
    }

    .btn-primary {
      background: linear-gradient(135deg, var(--ok), var(--ok-dark));
      color: #04100b;
    }

    .btn-secondary {
      border: 1px solid var(--border);
      background: rgba(255, 255, 255, 0.08);
      color: var(--text);
    }
  </style>
</head>
<body>
  <main class="success-card">
    <div class="icon">✓</div>
    <span class="badge">Completed</span>
    <h1>Message saved successfully</h1>
    <p>
      Your form was submitted and recorded. You can submit another message or return
      to the homepage.
    </p>

    <div class="actions">
      <button class="btn btn-primary" onclick="window.location.href='/'">New message</button>
      <button class="btn btn-secondary" onclick="history.back()">Go back</button>
    </div>
  </main>
</body>
</html>
